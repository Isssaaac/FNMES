@{
    Layout = null;
}
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />

    <title>工单列表</title>
    <link href="~/Content/layui/css/layui.css" rel="stylesheet" />
    <link href="~/Content/framework/css/console.css" rel="stylesheet" />
    <link href="~/Content/framework/css/animate.css" rel="stylesheet" />
    <link href="~/Content/font-awesome/css/font-awesome.min.css" rel="stylesheet" />
    <link href="~/Content/select2/css/select2.css" rel="stylesheet" />
    <!--[if lt IE 9]>
        <script src="/Content/jquery/jquery-1.9.1.min.js"></script>
    <![endif]-->
    <!--[if gte IE 9]><!-->
    <script src="~/Content/jquery/jquery.min.js"></script>
    <!--<![endif]-->
    <!--[if lt IE 9]>
    <script src="https://cdn.staticfile.org/html5shiv/r29/html5.min.js"></script>
    <script src="https://cdn.staticfile.org/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
    <script src="~/Content/layui/layui.js"></script>
    <script src="~/Content/select2/js/select2.min.js"></script>
    <script src="~/Content/framework/js/global.js"></script>
    <style>
        /* CSS样式代码 */
        th.hidden {
            display: none;
        }

        .search-row {
            display: flex;
            align-items: center;
            gap: 12px; /* 元素间距 */
            margin: 20px;
        }

        .search-input {
            width: 200px;
        }

        .search-icon {
            cursor: pointer;
            font-size: 20px;
            color: #1E9FFF;
        }
    </style>
</head>
<body>
    <div class="panel animated fadeIn">
        <div class="panel-body">
            <div class="layui-inline" style="margin-bottom: 10px;">
                

                
               @*  <div class="layui-btn-group">
                    <button onclick="export_btn()" class="layui-btn layui-btn-primary layui-btn-small">导出数据</button>
                </div>

                <div id="toolbar" class="FNMES-table-toolbar">
                    <button id="btnSearch" class="toolbar-search-button layui-btn layui-btn-normal layui-btn-small" style="line-height:20px">
                        <i class="layui-icon">&#xe615;</i>
                    </button>
                    <div class="toolbar-search-input">
                        
                    </div>
                </div> *@

                <div class="search-row">
                    <select lay-ignore name="lineId" class="select2" id="lineId" style="width: 250px;height: 30px;" lay-verify="required"></select>
                    <br></br>
                    <div class="layui-inline" id="ID-laydate-range">
                        <div class="layui-input-inline">
                            <input type="text" autocomplete="off" id="ID-laydate-start-date" style="width: 200px;height: 30px;" class="layui-input" placeholder="开始日期">
                        </div>
                        <div class="layui-input-inline">
                            <input type="text" autocomplete="off" id="ID-laydate-end-date" style="width: 200px;height: 30px;" class="layui-input" placeholder="结束日期">
                        </div>
                    </div>
                    <!-- 导出按钮 -->
                    <button class="layui-btn layui-btn-normal" id="export-btn" onclick="export_btn()">导出</button>

                    <!-- 文本输入框 -->
                    <input type="text" id="keyword" placeholder="key" autocomplete="off" class="layui-input" style="height:39px;width:300px">

                    <!-- 查询图标按钮 -->
                    <button class="layui-btn layui-btn-primary" id="btnSearch">
                        <i class="layui-icon layui-icon-search search-icon"></i>
                    </button>
                </div>



            </div>


            @* <div id="toolbar" class="FNMES-table-toolbar">

                <div class="layui-btn-group" id="queryDate">
                    <button value="1" class="layui-btn layui-btn-primary layui-btn-small">今天</button>
                    <button value="2" class="layui-btn layui-btn-primary layui-btn-small active">近7天</button>
                    <button value="3" class="layui-btn layui-btn-primary layui-btn-small">近1个月</button>
                    <button value="4" class="layui-btn layui-btn-primary layui-btn-small">近3个月</button>
                </div> 
                
                <button id="btnSearch" class="toolbar-search-button layui-btn layui-btn-normal layui-btn-small" style="line-height:20px">
                    <i class="layui-icon">&#xe615;</i>
                </button>
                <div class="toolbar-search-input">
                    <input type="text" id="keyWord" placeholder="key" autocomplete="off" class="layui-input" style="height:39px">
                </div>
            </div> *@
            <table id="gridList" class="layui-form layui-table table-hover FNMES-table" lay-skin="line">
                <thead>
                    <tr>
                        <th>
                            <input type="checkbox" lay-skin="primary">
                        </th>
                        <th>内控码</th>
                        <th>压降最大值</th>
                        <th>压降最小值</th>
                        <th>压降平均值</th>
                        <th>压降标准差</th>
                        <th>判定1上限</th>
                        <th>判定1下限</th>
                        <th>电芯条码</th>
                        <th>压降</th>
                        <th>a020测试时间</th>
                        <th>a020测试电压</th>
                        <th>m350测试时间</th>
                        <th>m350测试电压</th>
                        <th>间隔时间</th>
                        <th>间隔压降</th>
                        <th>判定1上限结果</th>
                        <th>判定1下限结果</th>
                        <th>判定1结果</th>
                        <th>判定2结果</th>
                        <th>结果</th>
                        <th>创建时间</th>
                    </tr>
                </thead>
                <!--内容容器-->
                <tbody id="content"></tbody>
            </table>
            <div id="paged"></div>
        </div>
    </div>
</body>
</html>

<!--内容模板-->
<script id="contentTpl" type="text/html">
      {{#  layui.each(d.list, function(index, item){ }}
    <tr>
        <td>
            <input type="checkbox" lay-skin="primary" value="{{item.Id}}"></td>
        <td>{{item.productCode}}</td>
        <td>{{item.maxVoltageDrop}}</td>
        <td>{{item.minVoltageDrop}}</td>
        <td>{{item.averageVoltageDrop}}</td>
        <td>{{item.stdDeviationVoltageDrop}}</td>
        <td>{{item.judgment1Up}}</td>
        <td>{{item.judgment1Lo}}</td>
        <td>{{item.cellCode}}</td>
        <td>{{item.voltageDrop}}</td>
        <td>{{item.a020TestTime}}</td>
        <td>{{item.a020TestVoltage}}</td>
        <td>{{item.m350TestTime}}</td>
        <td>{{item.m350TestVoltage}}</td>
        <td>{{item.timeInterval}}</td>
        <td>{{item.intervalVoltageDrop}}</td>
        <td>{{item.judgment1UpResult}}</td>
        <td>{{item.judgment1LoResult}}</td>
        <td>{{item.judgment1Result}}</td>
        <td>{{item.judgment2Result}}</td>
        <td>{{item.result}}</td>
        <td>{{item.createTime}}</td>
    </tr>
      {{#  }); }}
       {{# if(d.list.length<=0) { }}
          <tr style="color: red">
              <td colspan="8">查无数据。</td>
          </tr>
      {{# } }}
</script>

<script type="text/javascript">
    var paging;
    layui.config({
        base: parent._baseUrl
    }).use(['paging', 'form', 'layer', 'laydate'], function () {
        var layer = parent.layer || layui.layer;
        var form = layui.form;

        const today = new Date();
        const start = today.toISOString().slice(0, 10);
        const endDate = new Date(today.getTime() - 6 * 24 * 60 * 60 * 1000);
        const end = endDate.toISOString().slice(0, 10);

        document.getElementById('ID-laydate-start-date').value = end;
        document.getElementById('ID-laydate-end-date').value = start;

        var laydate = layui.laydate;
        // 日期范围 - 左右面板独立选择模式
        laydate.render({
            elem: '#ID-laydate-range',
            range: ['#ID-laydate-start-date', '#ID-laydate-end-date']
        });

        paging = layui.paging();
        $("#lineId").bindSelect({
            type: "POST",
            url: "/system/line/getListTreeSelect",
            title: '请选择线体'
        });
        $('#btnSearch').click(initGrid);
        $('#keyWord').bindEnterEvent(initGrid);
        // initGrid();
    });
    //$("#lineId").bindChangeEvent(initGrid);
    $("#lineId").on("select2:select", function (e) {
        // 处理选择事件
        if ($("#lineId").val() == "0") {
            return
        };
        initGrid();
    });

    function flush() {
        if ($("#lineId").val() == "0") {
            return
        };
        initGrid();
    }

    $('#queryDate').find('button').click(function (i, v) {
        $(this).addClass('active');
        $(this).siblings('button').removeClass('active');
        initGrid();
    });

    // function initGrid() {
    //     if ($("#lineId").val() == "0") {
    //         return
    //     };

    //     paging.init({
    //         url: '/record/selfdischarge/index',
    //         elem: '#content',
    //         tempElem: '#contentTpl',
    //         singleSelected: true, //单选
    //         params: {
    //             keyWord: $("#keyWord").val(),
    //             configId: $("#lineId").val(),
    //             index: $('#queryDate').find('button.active').val()
    //         },
    //         pageConfig: {
    //             elem: 'paged',
    //             pageSize: 10,
    //         },
    //         success: function () {

    //         }
    //     });
    // }

    function initGrid() {
        if ($("#lineId").val() == "0") {
            return
        };

        var startDate = $('#ID-laydate-start-date').val();
        var endDate = $('#ID-laydate-end-date').val();

        var stime = Date.parse(new Date(startDate));
        var etime = Date.parse(new Date(endDate));
        var usedTime = etime - stime;
        var days = Math.floor(usedTime / (24 * 3600 * 1000));

        //var days = $.calDays(startDate, endDate);
        if (days > 90) {

            $.layerMsg("日期间隔不能大于90天。", "warning");
            return
        }

        paging.init({
            url: '/record/selfdischarge/condition',
            elem: '#content',
            tempElem: '#contentTpl',
            singleSelected: true, //单选
            params: {
                configId: $("#lineId").val(),
                startDate: $('#ID-laydate-start-date').val(),
                endDate: $('#ID-laydate-end-date').val(),
                keyword: $('#keyword').val(),
                order: $('#order').val()
            },
            pageConfig: {
                elem: 'paged',
                pageSize: 10,
            },
            success: function () {

            }
        });
    }

    function export_btn() {
        if ($("#lineId").val() == "0") {
            $.layerMsg("请先选择线体。", "warning");
            return;
        }
        var startDate = $('#ID-laydate-start-date').val();
        var endDate = $('#ID-laydate-end-date').val();

        var stime = Date.parse(new Date(startDate));
        var etime = Date.parse(new Date(endDate));
        var usedTime = etime - stime;
        var days = Math.floor(usedTime / (24 * 3600 * 1000));

        if (days > 90) {
            $.layerMsg("日期间隔不能大于90天", "warning");
            return
        }
        $.layerMsg("正在请求导出...请耐心等待", "warning");
        //文件下载用fetch
        fetch("/record/selfdischarge/export?configId=" + $("#lineId").val() + "&startDate=" + startDate + "&endDate=" + endDate + "&keyword=" + $('#keyword').val(), {
            method: "GET",
        })
            .then(response => {
                // 检查响应状态是否成功
                if (!response.ok) {
                    return response.text();
                }
                return response.blob();
            })
            .then(blob => {
                if (typeof blob == "string") {
                    $.layerMsg(blob, "warning");
                    return
                }

                // 创建一个临时链接以下载文件
                const url = window.URL.createObjectURL(blob);
                const a = document.createElement("a");
                a.href = url;
                a.download = $("#lineId").val() + "-selfdischarge" + ".xlsx"; // 文件名
                document.body.appendChild(a);
                a.click();
                window.URL.revokeObjectURL(url);
            });
    }

    function export_outstation_btn() {
        if ($("#lineId").val() == "0") {
            $.layerMsg("请先选择线体。", "warning");
            return;
        }
        var startDate = $('#ID-laydate-start-date').val();
        var endDate = $('#ID-laydate-end-date').val();

        var stime = Date.parse(new Date(startDate));
        var etime = Date.parse(new Date(endDate));
        var usedTime = etime - stime;
        var days = Math.floor(usedTime / (24 * 3600 * 1000));

        if (days > 90) {
            $.layerMsg("日期间隔不能大于90天", "warning");
            return
        }
        $.layerMsg("正在请求导出...请耐心等待", "warning");
        //文件下载用fetch
        fetch("/record/selfdischarge/exportOutstation?configId=" + $("#lineId").val() + "&startDate=" + startDate + "&endDate=" + endDate + "&productCode=" + $('#productCode').val() + "&order=" + $('#order').val(), {
            method: "GET",
        })
            .then(response => {
                // 检查响应状态是否成功
                if (!response.ok) {
                    return response.text();
                }
                return response.blob();
            })
            .then(blob => {
                if (typeof blob == "string") {
                    $.layerMsg(blob, "warning");
                    return
                }

                // 创建一个临时链接以下载文件
                const url = window.URL.createObjectURL(blob);
                const a = document.createElement("a");
                a.href = url;
                a.download = $("#lineId").val() + "-OutstationData" + ".xlsx"; // 文件名
                document.body.appendChild(a);
                a.click();
                window.URL.revokeObjectURL(url);
            });
    }

</script>